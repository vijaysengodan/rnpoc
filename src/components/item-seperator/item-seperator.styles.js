import {StyleSheet} from 'react-native';

const itemSeperatorStyles = StyleSheet.create({
  lineStyle: {
    backgroundColor: '#CED0CE',
    width: 400,
    height: 1,
  },
});

export default itemSeperatorStyles;
